{
  "private": true,
  "scripts": {
    "dev:web": "npm --prefix apps/web run dev",
    "dev:landing": "npm --prefix apps/landing run dev",
    "build": "npm --prefix apps/web run build",
    "build:web": "npm --prefix apps/web run build",
    "build:replit": "npm run build:web",
    "install:replit": "npm ci --prefix apps/web && python3 -m pip install -r backend/requirements.txt",
    "build:landing": "npm --prefix apps/landing run build",
    "build:all": "npm run build:web && npm run build:landing",
    "lint": "npm --prefix apps/web run lint && npm --prefix apps/landing run lint",
    "preview:web": "npm --prefix apps/web run preview",
    "preview:landing": "npm --prefix apps/landing run preview",
    "security:check": "powershell -ExecutionPolicy Bypass -File scripts/security-check.ps1",
    "start:replit": "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}",
    "db:check": "tsx backend/node/checkDatabaseConnection.ts",
    "db:migrate": "python backend/scripts/run_migrations.py",
    "db:reset:local": "python backend/scripts/reset_local_database.py",
    "db:seed": "tsx backend/node/seedInitialWorkspace.ts",
    "db:backfill-workspaces": "tsx backend/node/backfillUserWorkspaces.ts",
    "classify:financial-data": "tsx backend/node/runSyncAndClassifyFinancialData.ts"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "axios": "^1.16.1",
    "googleapis": "^144.0.0",
    "pg": "^8.21.0",
    "recharts": "^3.8.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@types/pg": "^8.20.0",
    "tailwindcss": "^4.3.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
