# Replit editor: build the frontend, then start the single FastAPI web process.
run = "npm run build:replit && npm run start:replit"

[deployment]
build = "npm run install:replit && npm run build:replit"
run = "npm run start:replit"
